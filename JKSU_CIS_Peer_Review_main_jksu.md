# JKSU CIS 审稿意见：`main_jksu.tex`（传统蒙古文历史档案版面试点研究）

**审稿角色：** 模拟审稿人，《Journal of King Saud University — Computer and Information Sciences》  
**稿件：** `main_jksu.tex`（文本列检测与阅读顺序评估的试点研究）  
**日期：** 2026-05-14  

---

## 总体意见（推荐倾向）

**大修后再审**（若编辑部将「资源型/试点型短文」按长文录用标准放宽，则有可能降为 **小修**）。

稿件将问题界定清楚：在缺乏列级转写的前提下，聚焦**文本列检测与阅读顺序**，并诚实地报告**整页成功率极低**、不把 CER/WER 当作占位指标——这在文档分析与低资源场景中是负责任的做法。叙述上已明显从「benchmark」转向 **pilot / diagnostic**，与数据规模匹配。

但作为计算机与信息科学期刊的完整研究论文，**科学贡献仍主要落在「协议 + 小规模实证 + 工程接口」**，检测与排序本身多为既有流水线组合；若目标读者期待**可复现的大规模资源或明确的方法学进展**，当前版本在**统计效力、对照公平性、可复现细节**上仍容易被追问。

---

## 主要不足（Major）

### 1. 数据规模与划分对结论的支撑仍偏弱

- 全库 105 页、扩展测试 54 页，验证集仅 8 页；**阈值、几何先验均在极小验证集上选取**，再固定到测试集——审稿人通常会质疑**选择偏差与过拟合到 val 分布**。
- 分层分析在部分子组（如原文 11 页诊断表中的少列页仅 1 页）上**不具备统计意义**，虽正文已有「诊断性」说明，但主文仍承载大量表格，易被误读为稳定规律。

**建议：** 在讨论中更明确地写出「val 极小 → 所有超参选择的不确定性」；补充 **k 折交叉验证**、**留一机构/留一来源文件夹**（若元数据允许）、或至少 **bootstrap 在阈值选择上的敏感性**；若无法扩大数据，应更收缩全文对「规律总结」的语气。

### 2. 主结果与大量诊断实验的「评估域」不一致，影响可读性与可比性

- 主表为 **54 页扩展测试**，而迁移基线、IoU@0.75、训练规模曲线、AP、部分消融等仍锚在 **11 页 original test**。
- 这在方法上可辩护（历史对照、算力限制），但读者会困惑：**DocLayout-YOLO 50 epoch 等在扩展集上表现如何？** 若未算，应明确为「未在扩展集重评的原因 + 计划」。

**建议：** 优先在扩展测试上给出 **至少一组关键迁移/公平预算** 的数字（或明确声明仅保留 11 页为附录诊断并统一图表脚注），并在摘要/贡献中避免读者默认「所有表同一测试域」。

### 3. 「阅读顺序」方法学贡献有限且度量与召回耦合

- 排序规则为 **按列中心 x 坐标排序 + 旋转页反向**，属于透明但极简的启发式；与 Quirós & Vidal 等工作的差距未充分讨论。
- **配对式阅读顺序准确率**仅在匹配框上计算——正文已提醒与召回联合解读，这很好，但审稿人仍会问：**是否存在「漏检易列、保序难列」导致的乐观偏差？** 是否报告 **基于整页 GT 的排序**（即使预测框不完整）或 **order-aware 检测联合指标**？

**建议：** 增加与领域更可比的一种顺序评价（例如基于 GT 列序列与预测序列的 **编辑距离/最长公共子序列**，或仅在「检测足够完整」子集上报告）；或把当前度量明确定位为「检测后排序模块的 proxy」，并限制结论强度。

### 4. 标注一致性与 Ignore 区域仍是方法学薄弱点

- 5 页 IAA 中 **Ignore F1@0.5 很低**、阅读顺序一致性中等，说明 **噪声/印章/边界** 的标注协议仍在演化中；这对 FP 抑制与「忽略区过滤」相关结论有影响。
- 扩展测试上 **Ignore 过滤对 YOLO 在消融中未移除任何预测**——这既说明当前测试上该机制「未起作用」，也可能暗示 **预测与 Ignore 重叠定义或测试页构成** 值得再审视；审稿人可能认为消融信息量不足。

**建议：** 扩大双人标注与裁定案例展示；对 Ignore 协议给出 **更多可视化边界示例**；消融在 **Ignore 重叠更频繁的子集** 上单独报告。

### 5. 可复现性与实现细节披露不足（对应用型期刊尤其敏感）

- YOLO 训练 **batch size = 1**、Faster R-CNN 仅 **5 epoch** 等与 YOLO **50 epoch** 不对等——正文已写「诊断式比较」，但 **随机种子、数据增强、优化器、学习率调度、冻结层策略、负样本构成、NMS、多尺度策略** 等在主文仍偏概要。
- **Data Availability** 以政策限制为主，合理，但需更具体：**究竟共享哪些文件、版本号、哈希、脚本入口命令**，否则「可部分复现」难以落地。

**建议：** 补充一页 **Reproducibility checklist**（超参表、环境、随机性、训练日志摘要）；对 Faster R-CNN 等给出 **与 YOLO 算力对齐的扩展训练** 或明确声明资源限制并弱化对比句。

### 6. 对 JKSU CIS 读者「新意」的表述仍需警惕

- 贡献若仅为 **协议 + 小数据实验 + crop 导出**，在一般 CS 期刊中更常被定位为 **Short communication / Dataset & benchmark note**；当前篇幅与表格密度接近长文，**新意/技术深度**可能被质疑为 **「工程报告化」**。

**建议：** 在引言末段用两三句话**极清晰地区分**：（i）对蒙古文档案社区的新增知识；（ii）对通用文档分析方法的普适启示——若（ii）较弱，应主动收窄 claims，并考虑把大量诊断表移至补充材料以突出主线。

---

## 次要不足（Minor）

- **参考文献体量偏少**（尤其缺少近年 **layout detection / historical DIA / reading order** 的更系统覆盖），Related Work 可再扩展半页至一页。
- **伦理与数据治理**：历史档案虽非现代人体试验，但若涉及 **可识别个人信息、印章机构信息、文化敏感性**，可略作说明（脱敏策略、展示图是否已获准）。
- **统计报告**：除 bootstrap 外，可对 YOLO vs Faster R-CNN 的 F1 差异说明 **效应量很小 + CI 重叠**（若尚未写透可再压缩一句）。
- **表格密度**：主文表格很多，影响叙事流；部分诊断表可迁补充材料，主文保留 **1 张主结果 + 1 张严格页级 + 1 张不确定性**。
- **作者邮箱**：其一为 qq 邮箱对正式投稿并非硬伤，但部分编辑部偏好机构邮箱（非学术实质问题）。

---

## 改进建议（可操作的修订清单）

1. **统一或脚注化「54 页主评估 / 11 页诊断」**，减少跨表误读；优先补 **扩展集上的关键对照** 或诚实说明未重算原因。
2. **验证集极小** 背景下：增加 **交叉验证 / 多阈值敏感性曲线 on test**（声明 exploratory）或 **冻结一套事先登记的协议**（pre-registration 式简述）。
3. **阅读顺序**：补充一种 **整页或序列级** 指标，或引用并对比 1–2 个手写阅读顺序基线方法（即使失败也有信息）。
4. **标注与 Ignore**：扩大 IAA、展示分歧类型；讨论 Ignore 协议对 FP 定义的影响链。
5. **可复现**：环境、版本、命令行、共享产物清单；算力不对等的模型对比统一用 **「诊断」** 一词并减少并列排名式措辞（文中已做得较好，可再一致化）。
6. **叙事**：压缩诊断表在主文的占比，把贡献写成 **「可审计的评估包 + 坦诚的失败模式 + 下游接口」**，与期刊对应用论文的期望对齐。

---

## 结语

若编辑部将该稿定位为 **区域性文化遗产数字化 + 文档分析应用**，当前诚实度与问题意识是有竞争力的；若以 **通用机器学习/检测方法论文** 标准审稿，**新颖性与统计支撑**仍会是主要拒稿风险。按上述方向压缩宣称、加强评估域一致性、补强可复现与标注可信度，**大修后**更可能达到 JKSU CIS 的录用门槛。

---

*本文件由对话中的模拟审稿意见整理写入，非期刊正式决定。*
