# 传统蒙古文历史档案页面级标注指南

## 1. 标注目标

本项目标注目标是整页历史档案图像中的 **传统蒙古文手写文本列**。当前任务是页面级版面分析，不做文字转写。

## 2. TextColumn 文本列标注规则

当一个区域是目标传统蒙古文手写文本列时，标为 `TextColumn`。

规则：

1. 一个视觉上连续的竖写文本列标一个框。
2. 框应覆盖该列可见墨迹区域，但不要包含过多空白背景。
3. 如果一列文字中间有小断裂，但明显属于同一阅读列，可以标为一个框。
4. 如果多个片段空间分离明显，且可能对应不同列，应分别标框。
5. 印章、边框、污渍等不要并入 TextColumn，除非它们与文字严重重叠、无法分离。

## 3. 阅读顺序 reading_order

1. 按页面中实际阅读列顺序编号：`0, 1, 2, ...`。
2. 正常显示页面按统一列顺序编号。
3. 如果页面已旋转后再标注，则按旋转后显示坐标中的阅读顺序编号。
4. 如果两列顺序存在歧义，按页面主版式判断，并尽量在备注中说明。

## 4. 页面方向 orientation

- `correct`：页面已处于正确标注方向。
- `rotated_90_ccw`：原始扫描图需要逆时针旋转 90°。
- `rotated_90_cw`：原始扫描图需要顺时针旋转 90°。
- `rotated_180`：原始扫描图上下颠倒。
- `ambiguous`：方向无法可靠判断。

## 5. Ignore 区域定义

`Ignore` 只用于那些 **不应计入文本列误检** 的区域，尤其是容易被检测器误判为文本列的非目标区域。

建议细分类别：

1. **seal**：红色印章、戳记。
2. **stain_noise**：纸张污渍、墨迹渗透、强背景噪声、阴影。
3. **border_edge**：页边、扫描黑边、装订阴影、边框。
4. **non_target_language**：混入的中文手写档案或其他非目标文字。
5. **ambiguous_fragment**：过于残缺、无法判断是否属于完整文本列的碎片。
6. **overlap_unseparable**：与文字严重重叠、无法分离的干扰区域。

注意：

- 不要因为文字淡、难认就标为 Ignore；只要是目标蒙古文文本列，应标为 TextColumn。
- 不要用 Ignore 掩盖检测器错误。
- 如果区域中包含可读的目标文字，优先标 TextColumn。
- Ignore 应保守使用，主要用于评价时确实存在歧义的非目标区域。

## 6. 典型困难情况

### 6.1 印章覆盖文字

如果文字列仍可识别，应标 TextColumn；印章区域只有在可能造成误检时才额外标 Ignore。

### 6.2 强噪声

如果明显不是文字，且可能被检测为列，可标 Ignore。小型孤立污点不必全部标注。

### 6.3 横向或倒置页面

优先旋转到便于标注的方向，再在旋转后的图像坐标中标注，并记录方向信息。

### 6.4 中文档案页面

如果页面是中文手写档案而不是传统蒙古文，应从目标数据集中排除；若临时出现在标注工具中，应标记为 `non_target_language`，最终不进入评估。

## 7. 提交前检查

每页提交前检查：

1. 所有有效蒙古文文本列是否已标注。
2. reading_order 是否唯一且连续。
3. Ignore 是否过度使用。
4. 页面方向是否正确。
5. 中文或非目标档案页是否已排除。
