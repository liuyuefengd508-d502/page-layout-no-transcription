# 本轮评审意见修正记录

针对最新评审意见，已完成一轮可立即执行的稿件修正。

## 已修正的问题

1. **修复 LaTeX 未定义引用**
   - 补充 `pan2023molhw`、`deng2023yolo`、`zhao2024doclayout` 三条参考文献。
   - 新增 `tab:project_ap` 表，解决未定义交叉引用。

2. **补充 AP 结果表**
   - 新增 YOLOv8n project-level confidence-sweep AP：
     - AP@0.50 = 0.810
     - AP@0.75 = 0.321

3. **统一 YOLOv8n 960 设置的表格数值**
   - 将 inference-size 表中 960 行与主结果协议对齐：
     - Precision = 0.919
     - Recall = 0.626
     - F1 = 0.745
     - Mean IoU = 0.767
     - RO Acc. = 0.920
     - Mean s/page = 0.193

4. **弱化过强表述**
   - 将 “reproducible layout-analysis foundation” 改为更稳妥的 “initial, partially reproducible layout-analysis benchmark”。
   - Data Availability 中明确：需要获得档案图像访问权限时才能完整复现。

5. **解释 train_unlabeled 命名**
   - 正文中说明内部命名 `train_unlabeled` 是因为无文本转写；论文表格中改为 `train`，避免审稿人误解。

6. **修复排版警告**
   - 作者单位长行重排。
   - 主结果表使用 `\resizebox{\linewidth}{!}{...}` 防止超宽。
   - 定性图缩小为 `0.88\linewidth`，消除 float 过大警告。

## 编译状态

Overleaf 目录：
`/Users/liuyu/Desktop/mydocuments/codes/claudcodeTest/page_layout_no_transcription_overleaf`

已重新编译通过：
- `main.tex`
- `main.pdf`
- `main.log` 中不再出现 undefined citation/reference、overfull hbox 或 float too large 警告。

最新版 Overleaf 压缩包：
`/Users/liuyu/Desktop/mydocuments/codes/claudcodeTest/page_layout_no_transcription_overleaf.zip`

## 尚未解决、需要下一阶段实验处理的问题

1. 数据规模仍偏小，需要继续扩展页面数和测试集。
2. RT-DETR / DocLayout-YOLO 当前训练预算不公平，若作为正式强基线，需要重训到相近 epoch 或迭代数。
3. Ignore 标注一致性低，需要细化标注指南并扩展 IAA。
4. 当前方法创新仍偏弱，建议下一步优先设计 column-aware reading-order / layout graph 方法。
5. 若能找到蒙古文专家，建议补充 5–10 页小规模 OCR 转写验证。

## 继续补强：新增页面级严格指标与分组评估

本轮继续回应“评价指标不充分”的审稿意见，新增：

1. **页面级完整检测率** complete detection rate
   - 要求一页中所有有效文本列都匹配成功，且无 FP/FN。

2. **页面级完整成功率** full-page success rate
   - 在完整检测基础上，还要求 matched columns 的阅读顺序完全正确。

3. **阅读顺序相关性指标**
   - Kendall's tau
   - Spearman's rho

4. **分组评估**
   - few columns <= 5
   - medium columns 6--15
   - many columns > 15
   - rotated pages
   - pages with Ignore regions
   - no-Ignore page

新增脚本：
`/Users/liuyu/Desktop/mydocuments/codes/autoAIScienceforOCR/research_mongolian/page_level_ocr/compute_enhanced_layout_metrics.py`

新增结果文件：
`enhanced_layout_summary.csv`
`enhanced_layout_groups.csv`

论文中新增表格：
- `tab:enhanced_order`
- `tab:stratified_yolo`

编译状态：通过，`main.log` 无 undefined / overfull / float too large 警告。

## 继续补强：原始 TP/FP/FN 与 Ignore 敏感性透明化

针对评审意见“给出 TP/FP/FN 原始计数，便于验证 Precision/Recall/F1”以及“Ignore 区域可能影响 Precision”的问题，本轮新增：

1. **主结果原始计数表** `tab:main_counts`
   - 报告每个方法的 Pred / TP / FP / FN。
   - 所有方法均对 163 个非 Ignore GT 文本列评估。

2. **Ignore 敏感性说明**
   - 在 ablation 表中新增 `Removed` 列。
   - YOLOv8n 在 test split 中只有 3 个预测因与 Ignore 区域重叠而被移除。
   - 因此 Ignore 机制对 Precision 有影响，但不是主要决定因素。

3. **更新论文解释**
   - 明确 `w/o ignore filtering` 时 FP 从 9 增加到 12，F1 从 0.745 降至 0.736。
   - 明确 rotation-aware order 只影响 reading-order accuracy，不改变检测框。

编译状态：通过，`main.log` 无 undefined / overfull / float too large 警告。

## 继续补强：Baseline 训练预算与阈值协议透明化

针对评审意见“Baseline 训练和比较不够公平”的问题，本轮新增：

1. **训练预算与阈值协议小节**
   - 新增 `Training-Budget and Threshold Protocol` 小节。
   - 明确所有 test 阈值均来自 validation split，不在 test 上调参。

2. **新增训练协议表** `tab:training_protocol`
   - YOLOv8n：COCO/general detection 初始化，50 epochs，imgsz 960，val threshold 0.35。
   - Faster R-CNN MobileNetV3-FPN：COCO 初始化，5 epochs，max-side 960，val threshold 0.80。
   - RT-DETR-L：general detection 初始化，5 epochs，imgsz 640，val threshold 0.10。
   - DocLayout-YOLO：DocStructBench 初始化，5 epochs，imgsz 1024，val threshold 0.03。

3. **弱化 RT-DETR / DocLayout-YOLO 结论**
   - 明确这两个模型是 diagnostic transfer experiments，不是 fully optimized head-to-head comparisons。
   - Discussion 中说明：训练预算不等，因此不能把负结果解释为最终模型排名，只能说明直接迁移不容易。

编译状态：通过，`main.log` 无 undefined / overfull / float too large 警告。

## 继续补强：Column-aware post-processing 诊断实验

针对评审意见“方法创新性不足，仅是 YOLO + 简单排序”的问题，本轮增加一个诚实的结构先验诊断实验：

1. **新增方法小节** `Column-Aware Post-processing Diagnostic`
   - 对 YOLO 候选框增加传统蒙古文竖列结构先验：
     - confidence threshold
     - aspect ratio
     - relative height
     - relative width
   - 参数只在 validation split 上选择。

2. **新增实验脚本**
   `/Users/liuyu/Desktop/mydocuments/codes/autoAIScienceforOCR/research_mongolian/page_level_ocr/run_column_aware_yolo_grid.py`

3. **新增诊断结果表** `tab:column_aware_diag`
   - 主 YOLO threshold 0.35：test F1 = 0.745，RO Acc. = 0.920
   - recall-oriented threshold 0.30：test F1 = 0.769，但 RO Acc. 降至 0.830
   - column-aware prior filter：validation F1 = 0.817，但 test F1 降至 0.695，RO Acc. 提升到 0.965

4. **论文解释策略**
   - 不把 column-aware filter 包装为主方法提升。
   - 明确指出：手工几何先验容易过拟合小验证集，未来应发展 learned / data-adaptive column priors。
   - 这可以增强论文诚实性和方法讨论深度。

编译状态：通过，`main.log` 无 undefined / overfull / float too large 警告。

## 继续补强：标注协议与 Ignore 定义细化

针对评审意见“Ignore 区域定义薄弱、IAA 中 Ignore F1 低”的问题，本轮完成：

1. **新增英文标注指南**
   `Annotation_Guideline_Traditional_Mongolian_Page_Layout.md`

2. **新增中文标注指南**
   `传统蒙古文档案页面级标注指南.md`

3. **细化 Ignore 子类型**
   - seal：印章/戳记
   - stain_noise：污渍、墨迹渗透、背景噪声、阴影
   - border_edge：页边、扫描黑边、装订阴影
   - non_target_language：中文或其他非目标语言区域
   - ambiguous_fragment：过于残缺的疑似文字碎片
   - overlap_unseparable：文字与干扰物无法分离的重叠区域

4. **论文中新增 Ignore subtype 表** `tab:ignore_subtypes`
   - 明确这些子类型当前用于标注指导，不作为单独检测类别。
   - Discussion 中说明低 Ignore IAA 是限制，新的 subtype guideline 是后续提升一致性的措施。

编译状态：通过，`main.log` 无 undefined / overfull / float too large 警告。

## 继续补强：数据集分布统计

针对评审意见“没有说明页面来源、扫描分辨率、版式差异、噪声类型、列数分布等数据分布”的问题，本轮完成：

1. **新增数据集统计结果文件**
   - `dataset_statistics_summary.json`
   - `page_dataset_statistics.csv`

2. **新增论文表格** `tab:dataset_distribution`
   统计 62 页语料的：
   - 页面宽度/高度；
   - 每页文本列数量；
   - 每页 Ignore 区域数量；
   - 文本列框宽度/高度。

3. **关键统计**
   - 62 页；
   - 931 个有效文本列；
   - 153 个 Ignore 区域；
   - 16 页需要旋转或方向修正；
   - 38 页含至少一个 Ignore 区域；
   - 页面中位尺寸：6794 × 4830 px；
   - 每页文本列中位数：16；
   - 文本列框宽度中位数：252 px；
   - 文本列框高度中位数：3256 px。

4. **论文新增解释**
   - 明确当前数据集虽小，但页面布局密集；
   - 存在列数、干扰区域和扫描方向差异；
   - 这能更好支撑实验难度说明。

编译状态：通过，`main.log` 无 undefined / overfull / float too large 警告。

## 继续补强：按最新评审修正 Faster R-CNN 叙事与措辞

针对最新评审意见，本轮完成：

1. **弱化 OCR 闭环表述**
   - 将 `recognition-ready` 改为更稳妥的 `crop-export-ready interface`。
   - 避免暗示当前系统已完成端到端 OCR 验证。

2. **修正 Failure Analysis 文字错误**
   - 将 “four categories” 改为 “five categories”。

3. **规范规则方法命名**
   - 将 `Rule proposed` 改为 `Heuristic column extraction`。

4. **补充 Faster R-CNN 后续指标**
   因 Faster R-CNN 是当前 F1 最优模型，本轮将其加入：
   - IoU@0.75 strict-localization 表；
   - bootstrap confidence interval 表；
   - runtime 表。

   Faster R-CNN 补充结果：
   - IoU@0.75：P = 0.355, R = 0.264, F1 = 0.303, Mean IoU = 0.830, RO Acc. = 0.942。
   - Bootstrap F1 95% CI = [0.689, 0.879]。
   - CPU mean runtime = 0.168 s/page。

5. **Data Availability 更明确**
   - 明确可共享：annotation schema、annotation files、split manifests、evaluation scripts、trained-detector configuration notes、prediction files。
   - 明确原始档案图像受机构访问限制。

编译状态：通过，`main.log` 无 undefined / overfull / float too large 警告。

## 最后一轮小修：Faster R-CNN AP 与 pilot-scale 定位

针对最新评审意见，本轮完成：

1. **补充 Faster R-CNN AP**
   - 新增 Faster R-CNN AP@0.50 = 0.806。
   - 新增 Faster R-CNN AP@0.75 = 0.130。
   - 更新 `tab:project_ap`，现在同时报告 YOLOv8n 与 Faster R-CNN。
   - 结论更清楚：Faster R-CNN 在 IoU@0.5 F1 上更强，但严格边界 AP@0.75 低于 YOLOv8n。

2. **进一步弱化数据规模结论**
   - 摘要中加入 `pilot-scale`。
   - 贡献描述改为 `initial low-resource benchmark`。

3. **分组实验措辞更谨慎**
   - 明确 `tab:stratified_yolo` 是 diagnostic observations，而非稳定分组统计结论。

4. **结论中加入完整页面成功率为 0 的解释**
   - 明确当前系统应视为 `candidate crop generator`，不是 fully automatic page-level OCR preprocessing solution。

5. **Faster R-CNN 未充分调参说明**
   - Discussion 中新增说明：Faster R-CNN was not exhaustively optimized；其强结果说明 two-stage detection 值得后续 compute-matched 研究。

编译状态：通过，`main.log` 无 undefined / overfull / float too large 警告。
