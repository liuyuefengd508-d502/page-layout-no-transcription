# Q1 期刊补强实验执行计划

## 目标

把当前低资源页面级版面分析 pilot paper 进一步增强为更接近 Q1 期刊要求的完整实验稿。

## P0：强 baseline 对比

建议优先补 2 个强 baseline：

1. **Faster R-CNN / Detectron2**
   - 输入：当前 YOLO dataset 的 train/val/test split；
   - 输出：IoU@0.5、IoU@0.75、AP@0.50、AP@0.75、runtime；
   - 目的：提供经典 two-stage detector 对比。

2. **DocLayout-YOLO 或 LayoutParser transfer**
   - 如果能安装和适配，优先 DocLayout-YOLO；
   - 如果时间有限，则做 LayoutParser 预训练模型 zero-shot / fine-tune 迁移；
   - 目的：回应“文档专用模型对比不足”。

## P1：YOLO 训练消融

当前已经做了 inference-only imgsz 消融。若冲 Q1，应补真正 retraining 消融：

| 实验 | 设置 |
|---|---|
| image size | 640 / 960 / 1280 |
| augmentation | default / no augmentation / rotation-heavy |
| confidence policy | val-selected threshold / fixed 0.25 / fixed 0.35 |
| ignore handling | with / without ignore filtering |

每组至少报告：P、R、F1、AP@0.5、AP@0.75、runtime。

## P1：标注一致性 IAA

最低配置：

- 抽取 5 页：2 页普通页、1 页旋转页、1 页印章页、1 页少列页；
- 第二标注者独立标注 TextColumn 和 Ignore；
- 指标：
  - box-level mean best IoU；
  - matched box F1@0.5；
  - reading-order pairwise agreement；
  - ignore-region agreement。

如果没有蒙古文专家，IAA 仍然可以由非蒙古文人员完成，因为只标框和顺序，不转写文本。

## P1：数据扩展

建议把当前 62 页扩展到至少 100 页：

- train: 70--80 页；
- val: 10 页；
- test: 20 页；
- 保持 page-level split，避免同页泄漏；
- 继续排除中文手写档案页。

## P2：写作增强

1. Related Work 扩展到 25--30 篇；
2. 增加更多竖排文档和少资源文档版面分析引用；
3. 增加一张单页高清 success case；
4. 增加一张单页高清 failure case；
5. 补充代码与数据可用性说明。

## 最短冲刺路径

如果时间有限，建议按以下顺序：

1. IAA 5 页；
2. Faster R-CNN baseline；
3. YOLO retraining imgsz 640/960/1280；
4. 扩展 Related Work；
5. 更新论文和回复信。

预计时间：2--4 周。

## 2026-05-10 已完成：Faster R-CNN baseline

- 已完成 COCO-pretrained Faster R-CNN MobileNetV3-FPN fine-tuning。
- Val-selected threshold = 0.8。
- Test F1 = 0.782，Recall = 0.681。
- 该项已写入论文主结果表。

## 2026-05-12 当前状态更新

### 已完成到可投稿稿件的项目

1. **Faster R-CNN baseline**
   - Test F1 = 0.782；
   - AP@0.50 = 0.806，AP@0.75 = 0.130；
   - CPU mean runtime = 0.168 s/page；
   - 已进入 `main_jksu.tex` 主结果表。

2. **RT-DETR / DocLayout-YOLO transfer diagnostics**
   - RT-DETR-L test F1 = 0.058；
   - DocLayout-YOLO test F1 = 0.037；
   - 当前仅作为 negative transfer evidence，不作为公平强基线排名。

3. **IAA 5 页最低配置**
   - box F1@0.5 = 0.852；
   - mean matched IoU = 0.800；
   - reading-order agreement = 0.677；
   - Ignore F1 = 0.286；
   - 结论：TextColumn 标注一致性可接受，Ignore 标注仍需细化。

4. **JKSU CIS 投稿材料**
   - 主文：`main_jksu.tex` / `main_jksu.pdf`；
   - 补充材料：`supplementary_jksu.tex` / `supplementary_jksu.pdf`；
   - Cover letter、Highlights、major-revision summary、submission package 已生成。

5. **旧 62 页 reading-order 复核**
   - 已生成全量审计表、人工复核清单、62 张带编号复核图和 contact sheets；
   - 已通过 Label Studio Project 16 完成人工修正并合并回主标注；
   - 合并后 62 页共 904 个 TextColumn、122 个 Ignore；
   - 结构校验显示 0 个 reading_order 缺失、重复或不连续问题；
   - 快速回归指标已变化，下一步需完整重算 bootstrap、AP、IoU@0.75、消融与论文表格。

### 下一阶段真正冲 Q1 的优先级

1. **P0：扩展数据与测试集**
   - 从 62 页扩展到至少 100 页；
   - 测试集从 11 页扩展到至少 20--30 页；
   - 保持 page-level split，避免同页或同档案泄漏。

2. **P0：专家转写小样本 OCR case study**
   - 选 5--10 页或 50--100 个列 crop；
   - 报告 oracle crop vs detected crop CER/WER；
   - 只用于 case study，不把它包装成完整 OCR benchmark。

3. **P1：公平预算 detector 重训**
   - YOLOv8n、Faster R-CNN、RT-DETR、DocLayout-YOLO 使用可解释的统一预算；
   - 可选统一标准：相近 epoch、相近 iteration，或相近 wall-clock；
   - 报告 F1、AP@0.50、AP@0.75、runtime、page-level full success。

4. **P1：YOLO retraining 消融**
   - image size：640 / 960 / 1280；
   - augmentation：default / no augmentation / rotation-heavy；
   - threshold policy：val-selected / fixed 0.25 / fixed 0.35；
   - ignore handling：with / without ignore filtering。

5. **P1：方法创新**
   - 设计 column-aware layout graph 或 learned reading-order module；
   - 目标不是只调阈值，而是显式建模竖列、旋转、邻接关系和阅读顺序；
   - 验收指标至少包括 box F1、RO Acc.、full-page success。

6. **P2：扩展 IAA**
   - 从 5 页扩展到 10--15 页；
   - 增加 Ignore subtype 示例培训；
   - 分开报告 TextColumn、Ignore、reading-order agreement。
